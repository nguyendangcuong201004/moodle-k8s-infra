apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: moodle-efs-pvc
  namespace: moodle-production
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 50Gi
